---

nix_linux_dependencies:
  Debian:
    - curl
    - bzip2
    - ca-certificates
    - libdigest-sha-perl
  RedHat:
    - bzip2
    - perl-Digest-SHA

nix_daemon_path:
  Linux: "$HOME/.nix-profile/etc/profile.d/nix-daemon.sh"
  Darwin: "/nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh"
